<使い方>

・基本
  Step1: 
    "info_class"の before_func をグラフを描画したいファイルにimportする。
  Step2:
    任意のグラフを表示させる関数を作成する際、以下のように記述する。

    [例]
      @before_func("表示された引数に値を入力")
      def グラフを描画する関数( ~[dfなどの引数を入力] ,graph): # 引数の最後に"graph"を追加する。
          fig = graph.fig # いつもはgo.Figure() とかを書いてるところを左のように記述
          """
          ここより以下は普段通りに記述
          """

        return fig

  以上!

・応用
  他にもカスタマイズしたければ、グラフを描画するファイルに直接、before_funcを貼り付け、(パスの修正は各自に任せる)
  デコレータ関数内で行わせる処理にグラフ描画に必要なカスタマイズを追加する。
  # それ以降のグラフ描画までの流れは<基本同様に行えば良い>